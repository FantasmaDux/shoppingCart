spring:
    application:
        name: shopping-cart
    datasource:
      url: jdbc:postgresql://localhost:5432/shopping-cart-db
      username: postgres
      password: 267115
    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true
        properties:
          hibernate:
            dialect: org.hibernate.dialect.PostgreSQLDialect
            format_sql: true

api:
  prefix: /api/v1
